# {{ ansible_managed }}

[Unit]
Description=Traefik

[Service]
Type=notify
ExecStart=/usr/bin/traefik --configFile={{ traefik_config_directory }}/traefik.toml
Restart=always
WatchdogSec=1s

[Install]
WantedBy=multi-user.target
